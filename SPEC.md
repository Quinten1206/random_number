# 概率决策器 - 功能规格说明书 (spec.md)

## 1. 项目简介
一个简单的安卓离线工具应用。用户输入一个概率值，点击按钮后，应用以该概率随机输出“1”，否则输出“0”。应用无广告、不联网，专注单一功能。

## 2. 核心功能

### 2.1 输入概率
- 用户可在输入框中输入目标概率。
- 支持三种输入格式：
  - **小数**：0 到 1 之间的小数，例如 `0.3`、`0.75`、`1.0`、`0`。
  - **分数**：由 `/` 分隔的整数，例如 `1/2`、`3/4`，其值应在 [0,1] 内。
  - **百分比**：以 `%` 结尾的数值，例如 `50%`、`7.5%`，其代表的概率值应在 [0,1] 内。
- 输入框内允许包含合理的空格，如 `  0.5  `、`1 / 3`。

### 2.2 生成结果
- 界面有一个“生成”按钮。
- 用户点击按钮后：
  1. 解析输入内容，提取概率值 p（0 ≤ p ≤ 1）。
  2. 若输入非法，弹出提示，不产生结果。
  3. 若输入合法，生成一个随机数 r ∈ [0,1) 均匀分布。
     - 如果 r < p，结果输出“1”。
     - 否则输出“0”。
- 点击按钮后**不清空输入框**，概率值保留，用户可以多次点击重复随机生成。

### 2.3 结果显示
- 界面上有一个结果展示区域，清晰显示本次生成的数字（“1”或“0”）。
- 每次点击按钮，结果刷新为最新一次生成的值。

## 3. 输入验证与错误处理
应用必须对输入进行严格校验，出现以下情况时弹出提示，拒绝生成：

| 异常情况 | 提示说明示例 |
|---------|------------|
| 输入为空 | “请输入概率值” |
| 格式无法识别（非小数/分数/百分比） | “格式错误，请使用小数、分数或百分比” |
| 分数形式但分母为零 | “分母不能为零” |
| 最终计算出的概率值 < 0 或 > 1 | “概率必须在 0 到 1 之间” |
| 百分比数值部分无法解析 | “无效的百分比格式” |

提示方式：使用 Android 的 Toast 或对话框，简练明确。

## 4. 界面布局（概要）
界面采用简洁直观的设计，包含以下元素（具体实现待设计阶段细化）：
- **标题/说明文字**：提示用户输入概率。
- **输入框**：单行文本输入，支持小数、分数、百分比。
- **生成按钮**：居中或显眼位置。
- **结果展示区**：较大字号显示“1”或“0”，可配合颜色或轻微动画增强反馈。
- 所有元素布局在一屏内，无滚动需要，适合单手操作。

## 5. 非功能需求
- **离线运行**：应用不应请求任何网络权限，所有逻辑在本地完成。
- **无广告**：不包含任何广告 SDK 或推广内容。
- **轻量快速**：应用包体积小，启动迅速，随机生成延时不可察觉。
- **兼容性**：支持主流安卓版本（如 Android 7.0 及以上）。
- **隐私**：不收集、不存储任何用户数据。

## 6. 验收标准
1. 输入 `0.5`，连续点击生成按钮 100 次，输出“1”的次数约在 40~60 次之间（统计上 95% 置信区间）。
2. 输入 `1.0` 或 `100%` 或 `1/1`，每次点击均输出“1”。
3. 输入 `0` 或 `0%` 或 `0/2`，每次点击均输出“0”。
4. 输入 `abc` 或 `2` 或 `120%`，点击后出现明确错误提示，无结果输出。
5. 输入 `1/2` 后点击生成，结果仅可能是“1”或“0”，且连续点击时概率约为 50%。
6. 输入框内容在点击按钮后保持不动，可重复点击。
7. 应用在飞行模式下一切功能正常。

## 7. 限制与边界
- 只支持 Android 平台。
- 概率精度受限于双精度浮点数，对于极高精度需求（如 1e-12）不做额外保证。
- 单次生成，无批量生成或历史记录功能。
- 界面语言默认中文，不提供多语言切换。